/**
 * TODO: These tests were for screens.js which depends on the old audio.js.
 * The old audio.js has been replaced by the new layered audio system in /audio/.
 * Update screens.js to use the new audio system, then write new tests.
 */

import { describe, it } from 'vitest';

describe('screens', () => {
  it.todo('update screens.js to use new audio system and write tests');
});
