/**
 * TODO: These tests were for the old audio.js which has been replaced by the new
 * layered audio system in /audio/. Write new tests for:
 * - instrument-context.js (pitch conversion, tendency tracking)
 * - lenient-note-listener.js (note detection, event emission)
 */

import { describe, it } from 'vitest';

describe('audio', () => {
  it.todo('write tests for new audio system');
});
